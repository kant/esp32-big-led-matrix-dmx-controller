from setuptools import setup

setup(name='matrix_controller',
      version='0.1',
      description='Python library to control an ESP32 based LED matrix',
      license='MIT',
      packages=['matrix_controller'],
      zip_safe=False)